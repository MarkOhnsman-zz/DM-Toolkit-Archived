const actions = {
  create: 'Create',
  update: 'Update',
  remove: 'Remove',
  find: 'Find',
  findAll: 'Find All'
}

const models = {
  background: {
    name: 'Background',
    endpoint: 'backgrounds'
  },
}

module.exports = {
  actions,
  models
}